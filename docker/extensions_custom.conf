; This file is superseded.
;
; The local dev environment moved from a FreePBX-GUI Docker wrapper to a
; plain, actively maintained Asterisk image (andrius/asterisk) after the
; original tiredofit/freepbx image was found not to exist on Docker Hub.
;
; The real dialplan is now at docker/asterisk-conf/extensions.conf, mounted
; directly into the Asterisk container by docker/docker-compose.yml.
;
; This file can be deleted; it is kept only so anyone opening it understands
; where the real config moved rather than finding a dead, unused file.
