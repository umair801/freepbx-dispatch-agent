; docker/asterisk-conf/http.conf
;
; Asterisk's built-in HTTP server, required for ARI (REST + WebSocket) to be
; reachable at all. Without this enabled, ARI_BASE_URL in .env will refuse
; every connection regardless of ari.conf being correct.

[general]
enabled=yes
bindaddr=0.0.0.0
bindport=8088
