# AgAI-33 requirements.txt
# Trimmed from AgAI-7's full dependency set -- excludes chromadb, langchain-*,
# telegram, and other extras that belong to features AgAI-7 grew that are out
# of scope for this project (see project README "selective port" note).
# Version pins match AgAI-7 where the package is shared, so both projects can
# be developed against consistent, already-verified versions.

fastapi==0.135.1
uvicorn==0.41.0
pydantic==2.12.5
pydantic-settings==2.13.1

# Agent framework + LLM
langgraph==1.1.0
google-genai==1.66.0

# Database
supabase==2.28.0

# HTTP + retry
httpx==0.28.1
tenacity==9.1.4

# Asterisk ARI event stream (NEW in AgAI-33)
websockets==15.0.1

# Voice channel (Twilio kept for SMS/WhatsApp only, see core/config.py)
twilio==9.10.3

# Voice bridge -- TTS/STT for Asterisk calls (NEW in AgAI-33)
elevenlabs==2.42.0

# Email notifications
sendgrid==6.12.5

# Logging
structlog==25.5.0

# Env / config
python-dotenv==1.2.2
python-multipart==0.0.22

# Testing
pytest==9.0.2
pytest-asyncio==1.3.0
