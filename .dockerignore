.venv/
venv/
__pycache__/
*.pyc
.env
.git/
.pytest_cache/
docker/asterisk-data/

# Frontend is deployed separately as a static site (dispatch.datawebify.com);
# the backend Docker image doesn't need it.
dispatch-frontend/
